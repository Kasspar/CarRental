package org.example.carrental;

public enum CarType {
    /**
     * Represents the available car categories in the system.
     * Each category has its own inventory capacity.
     */
    SEDAN,
    SUV,
    VAN
}
